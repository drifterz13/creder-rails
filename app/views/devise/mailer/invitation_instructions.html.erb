<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <section class="max-w-2xl px-6 py-8 mx-auto bg-white dark:bg-gray-900">
        <main class="mt-8">
            <h2 class="text-gray-700 dark:text-gray-200"><%= t("devise.mailer.invitation_instructions.hello", email: @resource.email) %>,</h2>
            <p class="mt-2 leading-loose text-gray-600 dark:text-gray-300">
                <%= t("devise.mailer.invitation_instructions.someone_invited_you", url: root_url) %>
            </p>
            <div class="px-6 py-2 mt-4 text-sm font-medium tracking-wider text-white capitalize
                transition-colors duration-300 transform bg-blue-600 rounded-lg w-fit
                hover:bg-blue-500 focus:outline-none focus:ring focus:ring-blue-300 focus:ring-opacity-80"
            >
                <%= link_to t("devise.mailer.invitation_instructions.accept"), accept_invitation_url(@resource, invitation_token: @token) %>
            </div>
            <p class="mt-8 text-gray-600 dark:text-gray-300">
                Thanks, <br>
                Creder team
            </p>
        </main>
        <footer class="mt-8">
            <% if @resource.invitation_due_at %>
                <p class="text-gray-500 dark:text-gray-400">
                    <%= t("devise.mailer.invitation_instructions.accept_until",
                          due_date: l(@resource.invitation_due_at,
                          format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
                </p>
            <% end %>
            <p class="mt-3 text-gray-500 dark:text-gray-400">© <%= Time.current.year %> Creder. All Rights Reserved.</p>
        </footer>
    </section>
  </body>
</html>
